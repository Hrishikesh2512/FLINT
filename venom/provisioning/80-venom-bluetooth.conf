# Venom appliance: no login seats exist here — never gate Bluetooth on one.
# Without this, WirePlumber's bluez monitor stays offline on headless systems
# ("Seat state changed: offline") and BlueZ has no audio profiles to offer.
wireplumber.settings = {
  monitor.bluez.seat-monitoring = disabled
}
