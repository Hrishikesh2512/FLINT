# Venom laptop screen-text (OCR) server.
mss>=9.0
Pillow>=10.0
numpy>=1.26
# Fast native OCR on Windows (primary backend there).
winsdk>=1.0 ; sys_platform == "win32"
# Cross-platform OCR fallback (Linux/macOS, or if winsdk is missing).
rapidocr-onnxruntime>=1.3 ; sys_platform != "win32"
